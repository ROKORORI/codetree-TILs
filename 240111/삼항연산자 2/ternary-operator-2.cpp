#include <iostream>
#include <string>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int a;
    string out;

    cin >> a;
    out = (a == 1) ? "t" : "f";

    cout << out;
    return 0;
}